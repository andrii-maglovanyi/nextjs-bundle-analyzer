let prefix = ".next";
let budget = 200 * 1024;
export const setPrefix = (newPrefix)=>prefix = newPrefix;
export const getPrefix = ()=>prefix;
export const setBudget = (newBudget)=>budget = newBudget;
export const getBudget = ()=>budget;
